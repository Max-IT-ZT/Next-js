import { sum } from "./sum";

describe("Функія додавання двох чисел", () => {
  it("додати два числа", () => {
    expect(sum(2, 3)).toBe(5);
  });

  it("Працювати з негативними значеннями", () => {
    expect(sum(-1, -5)).toBe(-6);
  });
});
